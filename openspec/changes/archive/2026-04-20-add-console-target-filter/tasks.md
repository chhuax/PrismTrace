## 1. 控制台过滤入口与匹配配置

- [x] 1.1 在 `prismtrace-host` 控制台启动路径中增加目标过滤参数解析
  - 关联需求：控制台启动时必须支持限定监控目标
  - 验证：单元测试覆盖单个关键字、多关键字、未传过滤参数三类输入
  - 涉及文件：`crates/prismtrace-host/src/main.rs`

- [x] 1.2 建立控制台目标过滤配置与匹配语义
  - 关联需求：控制台启动时必须支持限定监控目标；控制台必须只展示匹配过滤条件的目标与聚合结果
  - 验证：单元测试覆盖空过滤、命中任一关键字、无匹配三类路径
  - 涉及文件：`crates/prismtrace-host/src/console.rs`

## 2. 过滤后的控制台聚合与页面上下文

- [x] 2.1 将目标过滤条件接入 target / activity / request / health 聚合结果
  - 关联需求：控制台必须只展示匹配过滤条件的目标与聚合结果；`local-console` 相关 modified requirements
  - 验证：聚合层测试覆盖 target 列表裁剪、activity/request/health 只返回匹配目标数据
  - 约束：不得改变底层 attach / detach 能力作用范围

- [x] 2.2 在首页与相关 payload 中展示过滤上下文
  - 关联需求：控制台必须展示当前过滤条件
  - 验证：首页渲染测试或集成测试验证过滤条件摘要可见；未传过滤参数时不出现误导性过滤提示

- [x] 2.3 增加无匹配目标时的控制台空态说明
  - 关联需求：无匹配目标时控制台必须保留可解释空态；`local-console` 相关 modified requirements
  - 验证：无匹配场景下主页可正常打开，Targets / Activity / Requests 保持与过滤上下文一致的空态

## 3. 验证与收尾

- [x] 3.1 补齐并运行与控制台目标过滤直接相关的自动化验证
  - 验证：运行新增/更新的 Rust 单元测试与集成测试，覆盖参数解析、过滤聚合、过滤上下文与无匹配空态
  - 关联需求：控制台启动时必须支持限定监控目标；控制台必须只展示匹配过滤条件的目标与聚合结果；控制台必须展示当前过滤条件；无匹配目标时控制台必须保留可解释空态

- [x] 3.2 完成黑盒回归与必要文档同步
  - 验证：blackbox-test 对应场景覆盖带过滤关键字启动、仅展示匹配目标、无匹配空态、未传过滤参数默认行为；同步 README / 使用说明中的过滤启动示例
  - 关联需求：控制台启动时必须支持限定监控目标；控制台必须展示当前过滤条件；无匹配目标时控制台必须保留可解释空态

## 备注

- 一次只推进一个 OpenSpec task。
- 本 change 只交付“启动时限定控制台监控目标”的最小能力，不在本轮扩展 UI 内动态筛选或底层 attach 范围控制。
- 只有在当前 task 的实现和验证都完成后，才将 `- [ ]` 改为 `- [x]`。
